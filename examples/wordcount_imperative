fn wordcount(words : [Int]) -> [Int * Int] {
  for ((word : Int) : words) {
    match map{word} with
    | l : () => { map{word} := 1; }
    | r : Int => { map{word} := r + 1; }
    end
  }
  return map;
}
